{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Brave",
            "url": "http://localhost:5173",
            "webRoot": "${workspaceFolder}",
            "runtimeExecutable": "/usr/bin/brave-browser"
        }
    ]
}